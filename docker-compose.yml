services:
  postgres:
    image: postgres:15-alpine
    container_name: todos-postgres
    command: >
      postgres
      -c max_connections=100
      -c shared_buffers=256MB
      -c work_mem=16MB
      -c effective_cache_size=1GB
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    expose:
      - "5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      # Using $$ escapes the variable so it evaluates inside the container
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - todos-net
    restart: unless-stopped

  postgres-exporter:
    image: prometheuscommunity/postgres-exporter:v0.15.0
    container_name: todos-postgres-exporter
    environment:
      DATA_SOURCE_NAME: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
    expose:
      - "9187"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - todos-net
    restart: unless-stopped

  pgbouncer:
    image: edoburu/pgbouncer:latest
    container_name: todos-pgbouncer
    environment:
      # Injected variables directly into the connection string
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      POOL_MODE: transaction
      DEFAULT_POOL_SIZE: 25
      MAX_CLIENT_CONN: 200
      AUTH_TYPE: scram-sha-256
    expose:
      - "5432"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - todos-net
    restart: unless-stopped

  pgbouncer-exporter:
    image: prometheuscommunity/pgbouncer-exporter:v0.8.0
    container_name: todos-pgbouncer-exporter
    environment:
      PGBOUNCER_URI: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@pgbouncer:5432/pgbouncer?sslmode=disable
    expose:
      - "9127"
    depends_on:
      pgbouncer:
        condition: service_started
    networks:
      - todos-net
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: todos-redis
    expose:
      - "6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes --save 60 1
    networks:
      - todos-net
    restart: unless-stopped

  redis-exporter:
    image: oliver006/redis_exporter:v1.61.0
    container_name: todos-redis-exporter
    environment:
      REDIS_ADDR: redis://redis:6379
    expose:
      - "9121"
    depends_on:
      redis:
        condition: service_started
    networks:
      - todos-net
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: todos-backend
    expose:
      - "8000"
    environment:
      # Backend connection string fully parameterized
      - DATABASE_URL=postgresql+psycopg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@pgbouncer:5432/${POSTGRES_DB}
      - REDIS_URL=redis://redis:6379/0
      - CORS_ORIGINS=${CORS_ORIGINS:-https://do.dharapx.work}
      - OAUTH_REDIRECT_BASE=${OAUTH_REDIRECT_BASE:-https://do.dharapx.work}
      - FRONTEND_URL=${FRONTEND_URL:-https://do.dharapx.work}
      - SECRET_KEY=${SECRET_KEY}
      - GITHUB_CLIENT_ID=${GITHUB_CLIENT_ID}
      - GITHUB_CLIENT_SECRET=${GITHUB_CLIENT_SECRET}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - OTEL_SERVICE_NAME=task-tracker-backend
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317
      - OTEL_TRACES_EXPORTER=otlp
      - OTEL_METRICS_EXPORTER=otlp
      - OTEL_LOGS_EXPORTER=otlp
      - OTEL_PYTHON_LOG_CORRELATION=true
      - OTEL_PYTHON_LOGGING_AUTO_INSTRUMENTATION_ENABLED=true
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.todos-backend.rule=Host(`do.dharapx.work`) && (PathPrefix(`/api/v1`) || PathPrefix(`/docs`) || PathPrefix(`/redoc`) || PathPrefix(`/openapi.json`))"
      - "traefik.http.routers.todos-backend.priority=100"
      - "traefik.http.routers.todos-backend.entrypoints=web"
      - "traefik.http.services.todos-backend.loadbalancer.server.port=8000"
      - "traefik.docker.network=dokploy-network"
    depends_on:
      postgres:
        condition: service_healthy
      pgbouncer:
        condition: service_started
      redis:
        condition: service_started
    networks:
      - todos-net
      - dokploy-network
      - obs-net
    restart: unless-stopped
    volumes:
      - uploads_data:/app/uploads

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-/api/v1}
    container_name: todos-frontend
    expose:
      - "3000"
    environment:
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-/api/v1}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.todos-frontend.rule=Host(`do.dharapx.work`)"
      - "traefik.http.routers.todos-frontend.priority=10"
      - "traefik.http.routers.todos-frontend.entrypoints=web"
      - "traefik.http.services.todos-frontend.loadbalancer.server.port=3000"
      - "traefik.docker.network=dokploy-network"
    depends_on:
      - backend
    networks:
      - todos-net
      - dokploy-network
    restart: unless-stopped

networks:
  todos-net:
    name: todos-net
    driver: bridge
  dokploy-network:
    external: true
  obs-net:
    external: true

volumes:
  postgres_data:
  redis_data:
  uploads_data:
